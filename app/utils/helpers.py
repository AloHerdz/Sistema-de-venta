# Este archivo se deja para funciones auxiliares futuras.
# Por ahora podemos dejarlo vacío o con alguna función genérica:

def format_date(date_obj):
    return date_obj.isoformat() if date_obj else None
